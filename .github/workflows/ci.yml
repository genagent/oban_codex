name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      MIX_ENV: test
    steps:
      - uses: actions/checkout@v6

      - uses: erlef/setup-beam@v1
        with:
          otp-version: "29"
          elixir-version: "1.20"

      - uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-

      # Dialyzer's PLT is expensive to build; cache it keyed on the toolchain and
      # deps so it only rebuilds when those change.
      - uses: actions/cache@v4
        with:
          path: priv/plts
          key: ${{ runner.os }}-plt-otp29-elixir1.20-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-plt-otp29-elixir1.20-

      - run: mix deps.get
      # Fail if a locked dep has been retired on Hex (security/yanked), so it
      # can't be silently reinstalled or republished against.
      - run: mix hex.audit
      - run: mix compile --warnings-as-errors
      - run: mix format --check-formatted
      - run: mix credo --strict
      - run: mix docs --warnings-as-errors
      # The :live tag (a real Codex call) is excluded by default, so CI
      # makes no model calls and needs no codex binary.
      - run: mix test
      - run: mix dialyzer
      # Run the fast, offline examples so a drift in the public API (or in
      # codex_wrapper's contract) breaks CI, not a user's first `mix run`. These
      # self-terminate in seconds with Codex stubbed. Excluded:
      # scheduled_routine.exs and agent_routine.exs (each waits ~70s for a Cron
      # tick), triage_issues.exs (shells to `gh`), console.exs
      # (interactive/paid), agent_live.exs, agent_retry_live.exs, and
      # agent_routine_live.exs (paid) -- all still parsed by
      # `mix format --check-formatted` above.
      - run: |
          for ex in playground propose_dispose event_driven agent_lifecycle; do
            echo "== examples/$ex.exs =="
            mix run "examples/$ex.exs"
          done
      # Packaging smoke test: build the Hex tarball (exercises the package/0
      # files: list) so tarball contents are validated in CI, not first at publish.
      - run: mix hex.build
